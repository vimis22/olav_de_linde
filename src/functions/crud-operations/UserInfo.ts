export type UserInfo = {
    id?: string,
    firstname?: string,
    lastname?: string,
    email?: string,
    password?: string,
    address?: string,
    housenumber?: string,
    role?: string,
    phone?: string,
};
