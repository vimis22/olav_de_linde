export type LogInfo = {
    id: string,
    streetname?: string,
    housenumber?: string,
    city?: string,
    zipcode?: string,
    country?: string,
    timestamp?: Date,
    message?: string,
}
